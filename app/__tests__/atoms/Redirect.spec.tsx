import { describe, it } from 'vitest'

describe('<Redirect />', () => {
  it('should render atom content correctly', () => {
    // TODO: test if children is displayed as expected
  })

  it('should redirect to an existing route', () => {
    // TODO: test if Link from @remix-run/react is working correctly
  })
})
